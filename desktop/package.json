{
  "name": "omnifin-desktop",
  "version": "0.1.0",
  "private": true,
  "description": "Native desktop client for an Omnifin server",
  "scripts": {
    "tauri": "tauri",
    "dev": "tauri dev",
    "build": "tauri build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  }
}
